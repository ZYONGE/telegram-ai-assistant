# 이 파일을 .env로 복사한 뒤 값을 채운다. .env는 git에 올라가지 않는다.

# BotFather에서 발급받은 봇 토큰
TELEGRAM_BOT_TOKEN=
# 사용자님 텔레그램 사용자 ID (숫자). 이 ID 외의 사용자는 무시한다.
TELEGRAM_ALLOWED_USER_ID=
# Anthropic API 키 (console.anthropic.com에서 발급)
ANTHROPIC_API_KEY=
